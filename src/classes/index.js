// Creamos un index dentro de clases donde manejaremos todas las importaciones para luego utilizar en el index gral

import { ToDo } from "./todo.class";
import { TodoList } from "./todo-list-class";

export{
    ToDo,
    TodoList
}